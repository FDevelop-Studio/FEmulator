name: FDevelop's Emulator Build

on:
  push:
    branches: [ "dev", "main" ]
  pull_request: 
    branches: [ "dev", "main" ]

jobs:
  build:
    name: Build and Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@4
      
    - name: Setup JDK
      uses: actions/setup-java@4
      with:
        distribution: 'temurin
